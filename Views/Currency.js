import { Button, Text,View} from "react-native";

const Currency = ({route,navigation}) => {
    return(
        <View>
            <Text>name:{route.mid}</Text>
        </View>
    );
};
export default Currency;